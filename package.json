{
  "name": "test-app",
  "version": "1.0.0",
  "main": "server/dist/server.js",
  "scripts": {
    "start": "node server/dist/server.js",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "square": "^33.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "concurrently": "^8.2.2",
    "typescript": "^5.4.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A restaurant order management application using Square API"
}
